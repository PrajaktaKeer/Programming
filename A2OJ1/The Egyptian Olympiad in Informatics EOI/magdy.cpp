#include<bits/stdc++.h>
using namespace std;

int main() {
	int x;
	cin>>x;
	if(x < 1)
		cout<<"Don't be lazy, it takes only few minutes, you can do it.";
	else if( x == 1)
		cout<<"Good Job";
	else 
		cout<<"You Rocks Man";
	return 0;
}